# Deferred fixes register — what "reproduce, don't improve" is deliberately preserving

**Status:** live index, opened 2026-07-20 (Stage 2-d). Update it in the same commit that reproduces
a wart, not later.

## Why this file exists

The build-modernization campaign's binding rule is **reproduce, don't improve**: CMake and Maven
must produce the same application autotools and Ant produce, warts included. That rule is right —
it keeps "did the build change?" separable from "did the product change?", which is the only reason
the parity harness can mean anything.

But it has an obvious failure mode: **faithfully reproduced defects become permanent** if nobody
writes them down. Until this file existed, they were recorded in three places with very different
survival odds — POM comments (good at stopping an accidental "fix", useless for enumeration),
design-doc prose (buried, per-stage), and the SDD progress ledger (**git-ignored scratch**, deleted
by `git clean -fdx`). Nobody could answer "what do we fix once the migration lands?"

**The rule for contributors:** when you reproduce something wrong because parity demands it, add a
row here in the same commit, and cite the file:line. A comment at the site is necessary but not
sufficient — the site comment stops the next reader from breaking parity; this file is what makes
the defect findable afterwards.

**Scope note.** Sections 1 and 2 are defects in *Scilab*. Section 3 is limitations in *our own
harness*. Section 4 is migration scaffolding that disappears by construction. Section 5 is
divergences accepted on purpose, forever. Keep them separate — they have different owners and
different "done" conditions.

---

## 1. Product defects — user-visible or shipped

These reach users. Highest priority once the migration lands.

| # | Defect | Evidence | Why reproduced | Proposed fix |
|---|---|---|---|---|
| P1 | **Every jar manifest carries a double-encoded vendor string** — `"Dassault SystÃ¨mes"` instead of `Systèmes`. Ant's `<property file>` read `scilab.properties`'s UTF-8 `è` (`c3 a8`) as Latin-1 and re-encoded, giving `c3 83 c2 a8`. | Any jar: `unzip -p modules/*/target/*.jar META-INF/MANIFEST.MF` | The manifest is inside the parity gate; emitting the correct string fails parity. The Maven port froze the mangled bytes verbatim in each `src/main/manifest/MANIFEST-section.MF` (extracted from the Ant jar, never retyped). | **RESOLVED 2026-07-23 — corrected + re-baselined.** Diagnosis refined: the mangling was **not** a live properties read (`scilab.properties`/`scilab.properties.in` already carry correct `c3 a8`, as do the `generated`-dimension `etc/Info.plist` copies) — it was frozen into the checked-in `MANIFEST-section.MF` files by the Ant→Maven extraction. A tree-wide byte scan proved the blast radius is **exactly** those files: 23 of 24 carry `c3 83 c2 a8` (2 lines each — `Specification-Vendor` + `Implementation-Vendor`); the 24th, `javasci`, has no vendor line at all (that is **B12**, untouched here). Applied a byte-level `c3 83 c2 a8 → c3 a8` to all 23. **Predict-then-diff:** rebuilt the 24 reactor jars (`mvn -o -DskipTests package` after deleting the stale jars — the jar plugin doesn't treat `manifestFile` as a staleness input, so a plain repackage no-ops), captured a candidate, and diffed: **exactly 23 `maven_jars` `META-INF/MANIFEST.MF` hash changes and nothing else** — every other jar entry, plus `dylibs`/`executables`/`generated`/`generated_cmake`/`header_defines`/`flags`/`tu_flag_facts`, byte-identical. Surgically re-baselined only those 23 hashes in `baseline-autotools.json` (46-line diff, `build_id` preserved). **Verified:** `PARITY OK`, full harness **211 passed, 1 skipped** (the skip is the forever-skipping `@_requires_ant_jars` align test). `java.util.jar.Manifest` decodes manifests as UTF-8, so `getImplementationVendor()`/`getSpecificationVendor()` now return `Dassault Systèmes` correctly. |
| P2 | **22 of 24 shipped jars have `Class-Path: ${manifest.class-path}`** — an uninterpolated Ant property literal, because only `gui` and `scirenderer` define the property and `build.incl.xml` emits the attribute unconditionally. | `build.incl.xml:159`; `unzip -p modules/types/jar/org.scilab.modules.types.jar META-INF/MANIFEST.MF` | Same reason as P1 — it is a manifest byte inside the gate. | **RESOLVED 2026-07-23 — literal Class-Path dropped.** Count refined: **21** of 24 jars carried the literal (not 22); the 3 with a real, interpolated Class-Path — `gui`, `javasci`, `scirenderer` — were left untouched. Removed the `Class-Path: ${manifest.class-path}` line from each of the 21 `src/main/manifest/MANIFEST-section.MF` files (the literal sits in the *named* section, where `Class-Path` is classloader-inert anyway — doubly junk). **Predict-then-diff:** rebuilt the reactor, captured, diffed — exactly 21 `maven_jars` `META-INF/MANIFEST.MF` hash changes and nothing else. Surgically re-baselined those 21 hashes. **Verified:** `PARITY OK`, 211 passed / 1 skipped. Fixed together with P4 (one bug in the unconditional stamping, seen twice). |
| P4 | **`graphic_objects`' jar ships `Specification-Title: ${library.title}`** — an uninterpolated Ant property literal, because `graphic_objects/build.xml` is the only real Java module that never defines `library.title`, and `build.incl.xml` stamps the attribute unconditionally. Same family as P2. | `unzip -p modules/graphic_objects/jar/org.scilab.modules.graphic_objects.jar META-INF/MANIFEST.MF` | Stage 2-e reproduced it verbatim; the manifest is inside the parity gate. | **RESOLVED 2026-07-23 — real title, not the literal.** Took the register's first option (a real title) over omission: every module but bare `javasci` has a meaningful `Specification-Title`, and the confirmed pattern is `Specification-Title == pom <name>` (graph → "Graph drawing", types → "Java compatible types", …). Set `graphic_objects`' title to **"Scilab graphic_objects module"** — its own pom `<name>`, itself derived (per that pom's header comment) from `build.xml:20`'s `<description>`, the authoritative label Ant provides for the one module lacking a `library.title`. Invented nothing. Bundled into the same predict-then-diff/re-baseline as P2 (graphic_objects is one of the 21); the pom header comment was updated to reflect the resolution. **Verified:** `PARITY OK`, 211 passed / 1 skipped. |
| P3 | **`scirenderer`'s manifest section names a package that does not exist in the jar.** Section header is `Name: org/scilab/modules/scirenderer/`; the classes are `org.scilab.forge.scirenderer.*`. Generated mechanically from `${ant.project.name}`, so the section is inert — `Package.getPackage("org.scilab.modules.scirenderer")` never resolves. | `unzip -p modules/scirenderer/jar/scirenderer.jar META-INF/MANIFEST.MF` | Stage 2-d reproduces it exactly; "correcting" it to the real package would be an improvement and a parity divergence. | **RESOLVED 2026-07-23 — pointed at the real package.** Consumer analysis first: the ONLY runtime reader of any package's manifest version is `org.scilab.modules.xcos` (`XcosDiagramCodec.java:302`, `CustomWriter.java:119`, both hardcoding that one package and stamping its version into saved `.xcos` diagrams); the dozen other `getPackage()` methods are the TabFactory pattern returning a package-name *string*, not `java.lang.Package` version reads. So scirenderer's section was **fully inert** — safe to correct. Renamed `Name: org/scilab/modules/scirenderer/` → `Name: org/scilab/forge/scirenderer/` (the jar's actual class package, confirmed by `unzip -l`) so the Spec/Impl metadata now attaches to a package that exists. **Predict-then-diff:** 1 `maven_jars` manifest change (scirenderer), re-baselined with B12. **Verified:** `PARITY OK`, 211/1. |

## 2. Build-system defects — not user-visible, but real

| # | Defect | Evidence | Why reproduced | Proposed fix |
|---|---|---|---|---|
| B1 | **`output_stream` is built by nothing.** It has a `build.xml`, `prebuildjava/build.xml` defines an unreachable target for it, its `Makefile.am` sets no `USEANT`, and `scilab-lib.properties:170-172` points at a jar no build produces (`modules/output_stream/jar/` does not exist). | `scilab-lib.properties:170-172`; `ls modules/output_stream/jar` → absent | A reactor forces the question; Stage 2-a chose to preserve today's behavior rather than answer it mid-migration. | **RESOLVED 2026-07-23 — dead Java entries removed.** The Ant `build.xml`/`Makefile.am`/`prebuildjava` target are all gone with the retirement; `output_stream` is now a native-only module (its C side builds under CMake). Confirmed the Java jar is truly dead — no reactor POM, absent from `etc/classpath.xml`, zero importers — and removed the 3 dangling `library.output_stream.*` entries from `scilab-lib.properties.in` (parity-free: `scilab-lib.properties` is deliberately outside the gate, per `capture.py`). |
| B2 | **`scirenderer` is Ant-only** — a `build.xml` with no `Makefile.am` and no `SUBDIRS` entry, reached solely through `prebuildjava`. | `modules/scirenderer/` has no `Makefile.am` | Same as B1. | **RESOLVED (moot) 2026-07-23.** The Ant-only shape is gone: the retirement deleted every module `build.xml`/`Makefile.am`, and `scirenderer` is a first-class Maven reactor module now (`modules/scirenderer/pom.xml` builds `scirenderer.jar`, loaded via `etc/classpath.xml`). |
| B3 | **`terminal` is GUI-gated and absent from the `prebuildjava` topo-sort.** It is the 24th jar but not in `prebuildjava/build.xml:25`'s 23-module `depends=` list; built only via its own `USEANT=1` path, gated `if GUI`. This is a fact about *Ant* and is permanent — out of scope for Maven to change (*reproduce, don't improve*). | `modules/terminal/Makefile.am:34` | Same as B1. | **RESOLVED for the Maven reactor (Wave F, `commit 44845dbf98c`):** `modules/terminal/pom.xml` added as the 24th `<module>` entry in the parent POM, byte-parity-green against the Ant jar. This closes the consequence the original row tracked — a shipping module the *reactor* never picked up — without touching the Ant-side fact in the Defect column, which stays true and unreproduced-away. Caught by a final review after Stage 2-f's Waves A-E had declared the reactor complete at 23/23; see `build-parity/tests/test_acceptance.py::_ant_modules_without_reactor_entry`, the bidirectional completeness check added in the same commit that closed this row. |
| B4 | **`debuglevel="lines,vars,source"` is dead text.** `build.incl.xml:129` reads `debug="${build.debug}"`, and `scilab.properties:3` sets `build.debug=off`, so `debuglevel` never applies — the real compile carries `-g:none`. | `build.incl.xml:129`, `scilab.properties:3` | Migrated POMs reproduce `-g:none`, which is what actually happens. | **RESOLVED (moot) 2026-07-23.** `build.incl.xml` was deleted by the Ant retirement (`find . -name build.incl.xml` → 0), taking the dead `debuglevel` attribute with it; the Maven build carries `-g:none` explicitly. |
| B5 | **`ivy.xml` is dead.** No ivy jar exists anywhere and `~/.ant/lib` does not exist, so `ant download` cannot run; its `<!-- COPY -->` entries are hand-copy instructions, not resolution. | `modules/prebuildjava/ivy.xml`; migration doc §"ivy.xml is dead" | Untouched during migration — deleting it is out of scope for a parity-gated stage. | **RESOLVED (moot) 2026-07-23.** `ivy.xml` was deleted by the retirement (`find . -name ivy.xml` → 0); Maven owns dependencies now. |
| B6 | **FlatLaf is fetched but unused.** `fetch-thirdparty.sh` pulls `flatlaf-3.4.1.jar`; its only reference in the tree is a comment. | `modules/ui_data/.../ScilabFileSelectorFilter.java:162` — `// TODO uncomment if using FlatLaf` | Not a build concern; recorded because the migration doc claimed it was "already bundled to replace the Swing L&F set", which overstated it. | **STILL OPEN — re-scoped 2026-07-23.** Re-verified properly (NOT the B13 comment-grep mirage): FlatLaf has **zero** real imports — `ScilabFileSelectorFilter.java:162` is a bare `// TODO uncomment if using FlatLaf`, still unused. What changed is the fetch: `fetch-thirdparty.sh` does **not** pull it — `flatlaf-3.4.1.jar` arrives inside the upstream **prerequirements tarball**, so "stop fetching it" is not ours to do, and "adopt it" is a UI/L&F decision outside the register's scope. Left open as a low-priority UI call, not a build defect. |
| B7 | **Cobertura is checked in but broken** — it calls `cobertura-*` tasks with no `taskdef`. | migration doc §5 | Out of scope for parity. | **RESOLVED (moot) 2026-07-23.** The broken `cobertura-*` tasks lived in `build.incl.xml`, deleted by the retirement. The leftover `cobertura-2.1.1.jar` ships inside the upstream prerequirements tarball (not `fetch-thirdparty.sh`, not git-tracked) — a harmless unused bundle jar, not ours to prune. |
| B8 | **JUnit is 4.10 with thin coverage** (~40 Java classes). | migration doc §1 | Test migration is its own Stage 2 sub-task. | **RESOLVED 2026-07-24/25.** Stood up JUnit **6.1.2** + surefire 3.5.6 + JaCoCo 0.8.15 in the reactor; ported the 40 legacy tests JUnit 4→6 into `src/test/java` (kept as a `-Pnative-tests` integration suite); then authored **~4583 hermetic unit tests across ALL 24 modules** via a multi-agent workflow (planner→parallel authors→verify), all green. Total instruction coverage 13.5%→**21.0%** — the pure-logic layer well-covered (types 82%, external_objects_java 71%, preferences 59%, graphic_objects 53%, graph 51%, commons 48%…); the Swing/SWIG-JNI layer sits at its hermetic floor (needs the native harness / a headless-GUI harness — separate efforts). See memory `scilab-java-test-suite`. |
| B9 | **`etc/classpath.xml` carries machine-absolute paths** and is generated by configure. | `etc/classpath.xml` | Regenerating it is a named later Stage 2 task. | **RESOLVED 2026-07-23 — dynamic CMake generation, `$SCILAB`-rooted.** New `cmake/ScilabClasspath.cmake` (wired into `CMakeLists.txt` after the RC-c generated files) regenerates `etc/classpath.xml` at configure time from `etc/classpath.xml.in`, resolving each of the 59 thirdparty `@VAR@` tokens to `$SCILAB/thirdparty/<jar>` by globbing the actual `thirdparty/` contents — version-agnostic (a `fetch-thirdparty.sh` bump flows through with no edit), and exactly one jar must match per token or configure fails loudly naming it (natives/sources/javadoc filtered; `[0-9]` disambiguates base-vs-prefixed families like lwjgl/lwjgl-vulkan). `LoadClassPath.java` already expands `$SCILAB`→`$SCI` at load, so the file is now machine-independent: identical bytes in the dev tree and every install prefix. This **closes the fresh-clone gap** (CMake never regenerated it before — it was frozen `configure` output, so a fresh clone had none) and **drops it from the `ScilabInstall.cmake` relocation set**. Kept as the runtime loader per migration doc §2a (the bespoke lazy `load="onUse"`/`disableUnderMode` mechanism is preserved, not replaced); the `.in` stays the structural template, and `@SCIRENDERER_CP@` — the one non-thirdparty token — resolves to its Maven **module** jar. Two faithful quirks reproduced: `@LUCENE_ANALYZER@`→`lucene-core` (folded in Lucene 9), `@JAXB_RUNTIME@`→`jaxb-api`. **Predict-then-diff:** the regenerated file differs from the frozen one in exactly the 59 absolute→`$SCILAB` lines (same jars/versions/order/attributes); parity showed one change — `generated/etc/classpath.xml` — surgically re-baselined. **Verified:** all 59 patterns validated 1:1 against `thirdparty/`, standalone `cmake -P` + in-build reconfigure both green, `PARITY OK`, 211 passed / 1 skipped. |
| B10 | **30 TUs silently lack `-fwrapv`** — the `_CFLAGS` footgun (automake per-target `_CFLAGS` REPLACES `$(AM_CFLAGS)`=`$(SCI_CFLAGS)` wholesale), across history_browser/parameters/preferences/string/… **RISK RE-ASSESSED 2026-07-23 — NOT a live correctness bug.** Measured against the baseline `tu_flag_facts`: **0** TUs are `-O2` without `-fwrapv`; all 30 `-fwrapv`-missing TUs are also `-O0` (`opt=O0 ∧ wrapv=False`). The signed-overflow UB miscompiles at `-O2`, so at `-O0` it is benign — the 2026-07-09 global `-fwrapv` hardening did cover every `-O2` TU. The earlier "highest-risk correctness exposure" wording overstated it. | baseline `tu_flag_facts`: `wrapv=False ∧ opt≠O0` → 0; `wrapv=False ∧ opt=O0` → 30 | The CMake port reproduces the footgun on purpose (parity, `flagfacts_check.py`). | **RESOLVED 2026-07-23 — un-footgunned.** Removed the `C_FLAGS_OVERRIDE -std=gnu23` from all 6 modules, so the 30 C TUs now get the full `SCI_CFLAGS` default (`-O2 -fwrapv -g1 -DNDEBUG`). The parity side is expressed in `flagfacts_check.py`'s `expected_for` (a derived C `_CFLAGS` footgun → tree default), leaving `capture_tu_flag_facts` FAITHFUL — so no re-baseline, its unit/fault-injection tests keep passing, and the gate now ENFORCES "no C TU stays footgunned." Restoring `-Werror` surfaced two deliberate const-discards in `pcre2_private.c` (`pcre2_split_pattern` returns non-const into its const input); dialed back with a per-source `-Wno-error=incompatible-pointer-types-discards-qualifiers` (the pre-footgun build's workaround) rather than an invasive const cascade. **Verified:** full `drop-in-all` build clean (193 targets, 0 errors), parity **211 passed**, 6 harness self-tests updated to the un-footgun semantics + 1 added, and a runtime smoke (strsubst/part/convstr/strindex/grep/pcre2 email+digit matches) confirms the O2 code is correct. |
| B12 | **`javasci`'s `build.xml` overrides the shared `jar` target**, so its jar has a bare manifest — no per-package `Name:` section, no `Specification-*`/`Implementation-*`, only a computed `Class-Path`. `Package.getImplementationVersion("org.scilab.modules.javasci")` therefore returns null at runtime. | `unzip -p modules/javasci/jar/org.scilab.modules.javasci.jar META-INF/MANIFEST.MF` (vs any other module) | Stage 2-f Wave B reproduced it exactly — its POM's manifest fragment is deliberately bare and carries **no** `Implementation-Version`, the one documented exception to that constraint. Adding one would fabricate content Ant never emits. | **RESOLVED 2026-07-23 — added a per-package section.** Consumer analysis (same as P3): no Scilab code reads javasci's version — only `org.scilab.modules.xcos`'s is consumed — so the null was inert *internally*, a completeness gap only an external javasci embedder would ever hit. Rather than switch javasci to the shared jar target (which would disturb its deliberately-custom **main-section** `Class-Path`, the embedding API's pointer to jvm.jar), **appended** a `Name: org/scilab/modules/javasci/` section — `Specification-Title: Scilab Java API` (its pom `<name>`, per the P4 pattern), Spec/Impl-Version/Vendor matching every other module — AFTER the untouched main-section Class-Path. Additive; the fabrication constraint was migration-era and no longer binds (same improve-now phase as P1/P2/P4). `getImplementationVersion("org.scilab.modules.javasci")` now returns non-null. **Predict-then-diff:** 1 `maven_jars` manifest change (javasci), re-baselined with P3. **Verified:** `PARITY OK`, 211/1. |
| ~~B13~~ | **RETRACTED 2026-07-23 — the survey was wrong.** The original claim ("completion, graphic_objects, graphic_export declare a JOGL dependency they never use") rested on `grep -li jogl modules/*/pom.xml` → 6, which matches **comment prose**, not `<dependency>` elements. Checked properly: `grep -cE '<artifactId>(jogl-all\|gluegen-rt)</artifactId>'` → JOGL is declared **only** in `gui` (2), `renderer` (2), `scirenderer` (2) — all three are real `com.jogamp` consumers. The three "dead" modules declare **zero** JOGL deps; their jogl mentions are accurate explanatory comments (POM-ordering analogies; graphic_export's use of the Scilab `JoGLView`/`implementation.jogl.*` classes via reactor siblings). **There is no dead-dependency cleanup** — the whole `opengl-removal.md` Phase 1 was a mirage. Lesson: never conflate a comment-matching grep with a dependency audit. | | | Nothing to do; row kept as a correction so the mistaken claim is not re-derived. |
| B14 | **25 Ant-era `modules/*/build/` directories (14M) linger in the dev tree and were being copied into every `.app`.** Untracked and gitignored, zero files newer than `pom.xml`, and no runtime reference (`etc/classpath.xml` has zero `/build/` entries) — pure leftovers from before the Ant retirement. | `find modules -maxdepth 2 -type d -name build` → 25; `git ls-files 'modules/*/build/*'` → 0; same 25 dirs found inside `/Applications/Scilab-2027.0.0.app` | Not reproduced — a genuine leak. The Ant retirement deleted `modules/*/jar/` but left the sibling `build/` output dirs, and `package-macos.sh` rsyncs the dev tree, so they rode into the bundle. | **RESOLVED 2026-07-23.** Two parts, both closed: (1) `package-macos.sh` excludes `modules/*/build/` so no bundle carries them; (2) the dev-tree copies have since been cleared — `find modules -type d -name build` → **0**, ~14M freed (an intervening `mvn`/clean removed them). They were pure Ant staging (Maven compiles to `target/`), so nothing regenerates them and the bundle exclusion now stands as belt-and-suspenders. |
| B11 | **Dead Makefile rules that collide with live ones** — `elementary_functions`' `libdummy`, and `CommandHistory_Wrap_Fake.c` (commented out by `config.status`). | RC-b live-rule filter | Not reproduced — worked around in the harness. Recorded so nobody "fixes" the filter without knowing why it exists. | **RESOLVED (moot) 2026-07-23.** The colliding dead rules lived in the autotools-generated Makefiles, all deleted by the retirement; the harness's live-rule filter and the frozen `tu_flag_facts` baseline preserved the behaviour they guarded. |
| B15 | **CLion runs the deleted autotools before every build.** `.idea/misc.xml` configures the project as a `GNU Autotools` Makefile project whose `preConfigureCommands` is `autoreconf --install --force --verbose "${PROJECT_DIR:-..}"; /bin/sh "${PROJECT_DIR:-..}/configure"`. The migration deleted `configure.ac`/`configure`, so the pre-config step now fails on load: `autoreconf: error: 'configure.ac' is required` → `Pre-configuration commands finished with code: 1`. Cosmetic — CMake configure/build is unaffected — but it is a red error on every project sync. | `.idea/misc.xml` (`buildSystemName="GNU Autotools"`, the `autoreconf` `preConfigureCommands`); the exact console error above | Not reproduced — a genuine leftover, and in **per-developer IDE state**: `.idea` is gitignored (0 files tracked), so this is not a repo artifact and cannot be fixed by a commit. | **RESOLVED 2026-07-25 — CLion now loads via CMake.** Fixed with CLion closed (a running JetBrains IDE rewrites `.idea/` from memory on exit and would revert an external edit); backups left at `.idea/*.pre-b15-backup`. (1) `.idea/misc.xml`: the `MakefileSettings` + `MakefileWorkspace` pair is GONE, replaced by `<component name="CMakeWorkspace" PROJECT_DIR="$PROJECT_DIR$/scilab" />` — the repo root has no `CMakeLists.txt`, which is *why* CLion had fallen back to the Autotools Makefile flavor. Active `preConfigureCommands` options: **0**. (2) `.idea/workspace.xml`: dropped the orphaned `MakefileLocalSettings` whose `projectSyncType` map still held `$PROJECT_DIR$/scilab → RE_IMPORT` (it could have re-triggered the Makefile import). **Verified:** both files parse as XML, no Makefile/Autotools component remains, and a plain `cmake -S scilab -B <scratch>` — exactly what CLion runs — configures **exit 0** (generates `version.h`/`machine.h`/`etc/classpath.xml`, finds OpenMP, arms the Maven jar bridge) with zero autotools involvement. **Guard rail added (the one real trap here):** a CLion profile must NEVER generate into `scilab/build-cmake`. That CLI tree carries an EMPTY `CMAKE_BUILD_TYPE`, and an IDE Debug profile would reconfigure it to `CMAKE_BUILD_TYPE=Debug`, flipping the very optimization flags (`-O2 -fwrapv`) that build-parity's per-TU `flagfacts_check` gate enforces — a green-looking IDE action that would break the parity gate. CLion keeps its own `cmake-build-*/`, now gitignored at **both** roots (rules proven to fire) so it never pollutes `git status`. **CONFIRMED IN THE IDE 2026-07-26 (user-verified):** CLion reopened with the pre-configuration error GONE and the CMake project loading — the fix also survived a full CLion restart (the IDE rewrote `workspace.xml` on exit without restoring `MakefileLocalSettings`, and left `misc.xml` untouched), which is the failure mode that made the closed-IDE precondition necessary. The `.idea/` edits themselves remain untracked per-developer state, as recorded. |
| B16 | **javasci's default mode and its link topology contradict each other, so the engine `exit(1)`s before any embedded-Scilab test can run.** `Scilab.java:67` defaults `advancedMode = false`, and a non-advanced open routes through `DisableInteractiveMode()` -> `setScilabMode(SCILAB_NWNI)` (`modules/call_scilab/src/c/call_scilab.c:62-65`). `checkForLinkerErrors()` (`modules/core/src/cpp/InitScilab.cpp:975-997`) then requires the DISABLED jvm lib in NWNI mode and calls **`exit(1)`** if it does not get it. The discriminator is pure link-time symbol resolution: `isItTheDisabledLib()` returns TRUE from `modules/jvm/src/nojvm/nojvm.c:109` (libscijvm-disable) and FALSE from `modules/jvm/src/c/JVM.c:314` (libscijvm). But `libjavasci2` links **libscilab**, the GUI aggregate, which carries the real `libscijvm` -> FALSE -> exit. **The two modes need opposite answers, so ONE libjavasci2 can never satisfy both**, and the legacy suite needs both: 14 tests construct `new Scilab()` (NWNI) and 5 construct `new Scilab(true)` (advanced). | Probe with SCI set and the farm on java.library.path: stderr `"Scilab should have 'libscijvm-disable' defined but gets 'libscijvm' instead"`, exit 1. `otool -L libjavasci2.2027.dylib` -> libscilab; `otool -L libscilab-cli.2027.dylib` -> **0** scijvm deps (it folds the -disable objects). Advanced mode (`new Scilab(true)`) passes the linker check and fails later and differently (`NoClassDefFoundError: org/scilab/modules/core/Scilab` — it wants the full `etc/classpath.xml` jar set), proving the current link is correct for ADVANCED mode and wrong for the NWNI default. | **Faithfully reproduced, not introduced.** `CMakeLists.txt:130-134` records that autotools did the same: libjavasci2 is a separate pkglib linking `libscilab.la`, and `modules/Makefile.am` ordered javasci last for exactly that reason. So these embedded-engine tests could never have passed in this configuration — consistent with their having rotted (they also carried 14 JUnit-4-only `@Test( expected = …)` annotations that stopped them even COMPILING; fixed 2026-07-26). | Not a one-line fix — it is an upstream design conflict. Relinking javasci2 to libscilab-cli would fix the 14 NWNI tests and break the 5 advanced ones; the architecturally correct answer mirrors the existing split and builds TWO variants (javasci2 + a cli-linked javasci2), which adds a pkglib artifact, new `dylibs` baseline entries and a second surefire execution. Also required for the advanced group: the full Scilab jar classpath. **Interim state (2026-07-26):** `-Pnative-tests` runs the legacy tests that need no engine (`org.scilab.tests.modules.action_binding.utils.SignalTest`, 2/2 green) and quarantines the engine-dependent packages (javasci, xcos, commons) behind `scilab.test.exclude.engine`, because they do not merely fail — **xcos HANGS** a forked JVM indefinitely, which would wedge CI. |
| B17 | **CMake never builds `libjavasci2` — a shipped, baseline-gated artifact — so the tree only still has one because a pre-migration autotools build left it behind.** The whole javasci module's native half is missing from the CMake migration: there is no `modules/javasci/CMakeLists.txt`, no `build-cmake/modules/javasci`, no javasci entry in `build-cmake/CMakeFiles/TargetDirectories.txt`, **zero** javasci mentions in the generated `build-cmake/Makefile`, and `cmake --build build-cmake --target help` lists no javasci target. The `libjavasci2.2027.dylib` sitting in `modules/javasci/.libs/` is dated **Jul 19 2026** while CMake-built siblings (e.g. `libscijvm.2027.dylib`) are Jul 26 — it is a stale leftover, and `.libs/` is untracked build output, so a fresh clone has nothing. | **Fault-injected and gated:** hide the stale dylib (exactly the fresh-clone state) and `parity.diff` reports `PARITY FAILED — dylib missing in candidate: libjavasci2.VER.dylib` (exit 1). The baseline's `dylibs` section does carry `libjavasci2.VER.dylib`, so parity passes today ONLY because the leftover lingers — the same frozen-artifact tautology that motivated the H5 harness retirement. | Not reproduced — a genuine migration MISS, and the fourth instance of vision-doc principle #5, *"builds here" != "builds on a clean clone"*: the artifact resolves locally, so every gate that walks `.libs/` stayed green while the build stopped producing it. Stage 1f's fold-in comment (`CMakeLists.txt:130-134`) explains why javasci needs no fold-in OBJECT lib and notes libjavasci2 is "a SEPARATE baseline artifact" — that separate artifact was then never given a target. | **RESOLVED 2026-07-26 — javasci2 is now a CMake target, byte-faithful.** New `modules/javasci/CMakeLists.txt` (declared from the top-level after `modules/`, since it links the libscilab AGGREGATE — same ordering reason as the executables). NOT `scilab_module()`: that helper requires GATEWAY_SOURCES and emits `libsci<name>`; javasci has no gateway and ships `libjavasci2`. It does REUSE the helper's flag/include env (`_scilab_module_flag_env` + `_scilab_module_apply`) so the TUs get the tree-default C facts the frozen `tu_flag_facts` expects (javasci has 0 overrides). Four findings the predict-then-diff surfaced, each fixed against measured evidence rather than assumption: (1) `src/c/DllmainJavasci.c` is wholly Windows-only (`WINAPI DllMain`, no POSIX guard) — EXCLUDED, confirmed by 0 DllMain symbols in the baseline artifact; (2) the baseline exports 885 symbols of which only 28 are JNI, the rest being autotools' folded api_scilab convenience lib — reproduced via `$<TARGET_OBJECTS:sciapi_scilab-obj>`; (3) libtool re-listed the WHOLE transitive closure as 60 DIRECT deps (measured: exactly libscilab + libscilab's own 59) — reproduced by deriving the list from the aggregate's `LINK_LIBRARIES` rather than transcribing 59 names that would drift, plus `-undefined dynamic_lookup`/`-no_fixup_chains` and the order-significant `/usr/lib` + gcc rpath pair with `SKIP_BUILD_RPATH`; (4) one EXTRA dep `@rpath/libc++.1.dylib` appeared because the folded C++ objects make CMake pick the clang++ driver where autotools used the C one — killed with `-nostdlib++`, exactly as `scilab_aggregate()` does. **Final state: 885/885 symbols with ZERO diff, 60/60 deps with ZERO diff, rpaths in order, `PARITY OK`.** Fresh-clone acceptance passed: deleting the `.libs` artifact entirely and rebuilding produces it from source with parity still OK, so the gap that motivated this row is closed at its root. **This UNBLOCKS B16** — the javasci link topology cannot be corrected on a library the build does not produce, and the new target's link line is exactly where B16's decision (libscilab vs libscilab-cli plus an explicit libscijvm-disable, mirroring how `scilab-cli-bin` links) belongs. |
| B18 | **The javasci engine tests, running for the first time, expose 8 assertion failures and one SEGFAULT.** With B16/B17 landed, 12 legacy classes / 51 tests now execute against a real embedded engine where previously ZERO did. Nine classes are fully green (testExistingVariable, testReadWriteInteger, testExec, testOpenClose, testBug7054, testExecException, testBug9149, testBug6651, testTypes — 35 tests). The rest: `testErrorManagement.getLastErrorCodeTest` expects error code 999 but gets **2**; `testBug4211` fails its single assertion; `testReadWriteBuf` fails **6 of 9**; and `testReadWrite` **CRASHES the forked JVM with `Segmentation fault: 11`**. | `mvn -Pnative-tests -pl modules/javasci test` after `sudo ln -s build-cmake/test-native-libs /usr/local/lib/scilab`; surefire reports `Crashed tests: org.scilab.tests.modules.javasci.testReadWrite`. | Not reproduced — NEWLY VISIBLE. These tests could not even compile until the JUnit-4 `@Test( expected=)` conversion, and could not RUN until B16/B17, so nothing here is a regression: it is the first look at behaviour that has been unexercised for years. | **SEGFAULT ROOT-CAUSED 2026-07-27 — and it is NOT marshalling.** Bisected to `testReadWrite.putAndGetListTest`, then reduced to a 6-line reproducer: `sci.put("a", mlist)` where the `ScilabMList` holds ANY leaf, followed by `sci.close()`. put and get both SUCCEED; the crash is in engine TEARDOWN. An empty MList closes fine — the leaf is what matters — and the outer list is irrelevant. Crash stack (macOS report): `types::List::~List()` → `killMe` → `types::MList::~MList()` → `Overload::generateNameAndCall` → `Overload::buildOverloadName` → `types::TList::getTypeStr()` → `wcslen` → SIGSEGV. **Mechanism:** `MList::~MList()` (modules/ast/src/cpp/types/mlist.cpp:42) deliberately invokes the user-overloadable `%<type>_clear` on destruction, which needs the type name; `TList::getTypeStr()` (tlist.cpp:288) guards only on `getSize() < 1` and then calls `getFieldNames()->get(0)`, while `getFieldNames()` (tlist.cpp:313-316) is an **UNCHECKED** `(*m_plData)[0]->getAs<types::String>()`. A well-formed Scilab mlist keeps its type-name String at element 0, but the **Java API lets callers build an MList with no such header** — element 0 is then a Double, `getAs<String>` yields a bogus pointer, and `get(0)` reads a garbage `wchar_t*`. The empty-vs-leaf split follows exactly: size 0 short-circuits to `L""`, size ≥ 1 walks into the bad read. **FIXED 2026-07-27.** `TList::getTypeStr()` now validates element 0 (non-null AND `isString()`, then a non-empty String) and returns `L""` for a malformed t/mlist, so `Overload` falls back to the generic name instead of faulting — the engine must not crash on input a public API can construct. **Verified:** all three reproducer shapes (`mlist[leaf]`, `list[mlist[leaf]]`, empty) now exit 0 where two segfaulted; `testReadWrite` runs its full **15** tests (it previously crashed having completed none); the javasci native suite goes from 12 classes/51 tests **with a crash** to **13 classes / 94 tests / 0 crashes**. `PARITY OK` + flagfacts_check rc=0 (an internal branch moves no exported symbol) and the default `mvn test` is unchanged at 28/28. **Verification trap worth remembering:** the first re-test still segfaulted because the farm resolves through `modules/*/.libs/`, which only the `drop-in-*` targets refresh — the rebuilt engine sat in `build-cmake/` while the test kept loading the previous day's dylib. Always `drop-in-all` before re-testing a native fix. Consider also rejecting/normalising a header-less MList at the javasci boundary, but harden the engine first. NOTE this is a genuine PRODUCT bug reachable from the documented Java API, not a test artifact. `testReadWriteBuf`'s 6 failures are a SEPARATE matter (the direct-buffer `putAndGetRef*` path) and remain untriaged. The 999-vs-2 error code looks like a stale expectation rather than a product fault, but confirm against `lasterror` semantics before touching the test. NOTE the harness prerequisite discovered here: `<reuseForks>false</reuseForks>` is now set because the Scilab engine is NOT re-entrant within a process — with surefire's default fork reuse a second engine open segfaulted the JVM and silently TRUNCATED the run after 3 classes, reporting BUILD FAILURE with no indication that 9 classes never ran. |
| B19 | **SIGTERM did not terminate Scilab — only SIGKILL did, so anything stopping it the normal way hung ("needs a force quit").** SIGTERM sat in `base_error_init`'s FATAL signal list next to SIGSEGV/SIGBUS/SIGILL, all routed to `sig_fatal()` (`modules/core/src/c/signal_mgmt.c`). Three faults compounded: (1) `sig_fatal()` printed *"A fatal error has been detected by Scilab"* plus a backtrace — SIGTERM is not an error; (2) its SIGTERM case ran `kill(-getpgrp(), SIGTERM)`, and a process's own group INCLUDES itself, so the handler re-entered; (3) it ended in `longjmp(ScilabJmpEnv, HUGE_ERROR)`, which RESUMES the interpreter — so the process carried on running. None of `backtrace_print`/`Scierror`/`longjmp` is async-signal-safe either. | Reported by the user as the app needing a force quit. Measured: a `gtimeout 300` child was still alive **8h41m** after its SIGTERM, and an explicit `kill -TERM` also failed. A wedged `tbx-verify-all.sh` had been stuck since 02:19 for exactly this reason — `timeout` kills by SIGTERM, so every per-toolbox child hung forever and the loop never advanced. | Not reproduced — a genuine defect, and one that bites any supervisor (`timeout`, CI job control, launchd, a shell `kill`), not just interactive use. | **FIXED 2026-07-27.** SIGTERM removed from the fatal list and given its own `sig_term()` handler, which keeps the original documented intent (take down the Scilab process group so `host()`/spawncommand forks do not outlive us) but resets the disposition to `SIG_DFL` FIRST so the group broadcast cannot re-enter, then `_exit(128 + SIGTERM)` — async-signal-safe, unlike `exit()`, and the conventional 143. **Verified:** a sleeping `scilab-adv-cli` now dies on SIGTERM with rc=**143** where the same signal previously left it running; `exit(7)`→7 and `exit(0)`→0 still honored with output intact; the SIGSEGV/fatal path is untouched; `PARITY OK`. NOTE this fixes SIGNAL-based termination; if the GUI still hangs on Cmd-Q that is a separate JVM/shutdown-hook matter. |

## 3. Harness limitations — gaps in our own gate

Not Scilab defects. These bound what parity can currently prove; each fails **red** or **silent** as noted, and silent ones are the dangerous kind.

| # | Limitation | Fails | Fix when |
|---|---|---|---|
| H1 | **`maven_jars` is a permanent skip in CI.** The pytest job runs on `debian:bookworm-slim` (self-skips); the native parity job runs `capture`+`diff`, not pytest; **no CI job runs `mvn` at all**. | Silent | Maven is wired into CI — pair the alignment test with the completeness assertion, or the dimension goes dormant exactly where it matters most. |
| H2 | **Completeness is module-granular** — `_missing_reactor_jars` asks only whether a module produced ≥1 Maven jar. A module whose Ant build emits two jars and Maven one would pass. | Silent | A module emits more than one jar (none do today). |
| H3 | **Profile-conditional `<modules>` are invisible** to the completeness parse (`root.findall("{ns}modules/{ns}module")` matches only a direct child of `<project>`). | Silent | Migrations get gated behind an opt-in profile. Update the exact-list test; **never** weaken it to a truthiness check. |
| H4 | **Nested `target/` is invisible** — only `modules/<m>/target` is walked, so an extra artifact at `modules/<m>/sub/target/` escapes. A *declared* nested reactor module fails loudly via completeness; only extra artifacts are silent. | Silent (extras only) | A multi-artifact module appears. Do **not** broaden the walk without re-reading the laundering bug it would reintroduce. |
| H5 | ~~**`flags` captures only global per-language flags** — per-TU overrides (e.g. `colnew.f` forced to `-O0`) are invisible to it. Superseded in practice by RC-b's `tu_flag_facts`.~~ | ~~Silent~~ | **RESOLVED 2026-07-25 — retired.** The coarse `flags` fingerprint dimension, the tautological `tu_flag_facts` *dimension*, and the `parity/makeflags.py` Makefile-deriver were all removed. Each re-read a **frozen autotools artifact** at capture time (`config.status` / the generated Makefiles — both git-ignored, absent on a fresh checkout), so they only ever compared frozen-vs-frozen where stale artifacts lingered in a working tree; and being derived from `$(SCI_CFLAGS)`, `flags` could not gate `-std=` at all (autotools spells std in `$(CC)`), so on a clean post-autotools CI checkout `parity.diff` would have **FAILED** on `flags`/`tu_flag_facts`. `parity/flagfacts_check.py` — per-TU, reading the frozen baseline `tu_flag_facts` + `compile_commands.json`, never any autotools file — is now the sole, strictly stronger flag gate. The `-fwrapv` "sat-green-for-days" fault-injection acceptance was rewritten to fault-inject the **real CMake compile DB** through that gate. Net **−1089** harness lines (`makeflags.py` + 2 test files deleted); baseline's vestigial `flags` block dropped, `tu_flag_facts` kept (the gate's frozen ground truth); full harness suite green; fresh-checkout decoupling proven (capture+diff `PARITY OK` with `config.status` hidden). |
| H6 | **Manifest comparison cannot distinguish "attribute absent" from "attribute present with a volatile value"** — `normalize_manifest` strips volatile lines from both sides. This is how P-class defect Stage 2-c Critical 1 hid: the template dropped `Implementation-Version`, the harness saw no difference, and `xcos` would have written `null` into user diagrams. | Silent | Now mitigated by convention (every manifest fragment carries a frozen `00000000 0000`), not by the harness. A structural fix would compare attribute *presence* separately from value. |

## 4. Migration scaffolding — removed by construction

Recorded only so nobody mistakes them for permanent design. All disappear at the endgame
(**autotools deleted** — migration doc §12).

- Ant and Maven both build every jar; Ant is the real build, Maven is run by hand.
- Maven writes to `modules/<m>/target/`, not `modules/<m>/jar/`, deliberately — a shared directory
  would let a stray `mvn` run feed CMake a Maven jar undetectably. The directory flips at the CMake swap.
- `<scope>system</scope>` + `<systemPath>` for permanently-vendored jars. Correct today and
  immune to mirror interception, but `system` scope is deprecated; a Maven 4 move needs a different
  mechanism (an internal repository, or `build-helper:attach-artifact`).
- The `config.status` version-triple dependency, which blocks RC-e (deleting `./configure`).

## 5. Accepted divergences — differences that are permanent and harmless

Not a defect in Scilab, not a harness gap, and not scaffolding that disappears at the endgame.
These are places where Maven's output provably differs from Ant's at the byte level, **forever**,
with no behavioral consequence — so parity is measured on the reconstituted *meaning*, not the raw
bytes that encode it.

| # | Divergence | Why accepted | Harness handling | Sensitivity proof |
|---|---|---|---|---|
| A1 | **Long manifest attribute values wrap at a different byte offset.** Ant's `org.apache.tools.ant.taskdefs.Manifest` breaks continuation lines at 70 bytes (`MAX_LINE_LENGTH - 2`, reserving room for the trailing CRLF); Maven's archiver stack (maven-archiver/plexus-archiver) breaks at the full 72. **Neither writes through `java.util.jar.Manifest`** — an earlier draft of the Stage 2-d doc claimed they did and would "therefore" agree; a reviewer measured otherwise. Reproduced with `gui`'s real six-entry `Class-Path` (`flexdock.jar jrosetta-engine.jar jrosetta-API.jar javafx.base.jar javafx.swing.jar javafx.graphics.jar`, 114 bytes): Ant breaks mid-token at `javafx.b` / `ase.jar`, Maven at `javafx.bas` / `e.jar`. | A manifest's meaning is `{attribute: value}`; the wrap position is a serialization artifact of the 72-byte-per-line limit, invisible to every real consumer — `java.util.jar.Manifest` reconstitutes the logical value when a jar is read, so the JVM classloader resolving `Class-Path` never sees where the writer broke the line. **No POM content can change either break position** — verified by feeding Maven both the pre-wrapped and the unwrapped form of the same value and getting the identical 72-byte break both times, so there is nothing to fix on our side. | `normalize_manifest` (`build-parity/parity/fingerprint.py`) now joins continuation lines — a single leading space starts a continuation, stripped when joined — **before** the volatile-line filter, then compares the reconstituted value instead of the literal bytes. A correction, not a weakening: it only makes two *different* wrap positions of the *same* value compare equal. | `build-parity/tests/test_jar.py`: `test_normalize_manifest_same_value_different_wrap_position_compares_equal` (same value, different wrap → equal) plus three siblings (`..._changed_value_still_caught_when_wrapped`, `..._removed_attribute_still_caught_when_wrapped`, `..._added_attribute_still_caught_when_wrapped`) proving a changed, removed, or added wrapped attribute still fails. |

---

## 5a2. `machine.h` / `version.h` consumption cutover — RESOLVED 2026-07-23

Found while cleaning stale "hybrid coexistence" labels; fixed the same day. RC-a made CMake
**compute** `machine.h`/`version.h` into `build-cmake/generated-includes/` and PREPEND that dir to
the module include path (`ScilabConfigure.cmake`), the intent being that the compile consumes the
generated copies. It didn't — the compile fell through to the *source*
`modules/core/includes/{machine.h,version.h}`, which are **gitignored and untracked**, produced only
by the now-deleted `./configure`. A fresh clone has neither file, so it could not build.

**The real blocker was not the include path — it was that the generated `machine.h` did not compile.**
`cmake/machine.h.cmake.in` carried CMake-porting rationale in `/* … */` comments, and two token
classes leaked through `configure_file`:

1. the literal **`#cmakedefine`** in comment prose (10 lines across the CURL/LIBARCHIVE/LIBXML/
   PACKAGE_* blocks) — `configure_file` treats it as a directive and rewrote e.g.
   `/* Plain #define, not #cmakedefine … */` into `/* #undef  */`, spilling the rest as bare C tokens;
2. a comment that spelled out C block-comment delimiters literally (`` `/* ` `` … `` ` */` ``) — the
   embedded `*/` closed the comment early.

So the generated header had syntax errors (`unknown type name 'different'` at line 33), which is why
the compile silently kept using the source copy and why the cutover looked "done but inert."

**Fix:** stripped the `#` from `#cmakedefine` on comment lines only (never on the 169 real
directives, which start at column 0), and reworded the delimiter-spelling comment. The generated
`machine.h` now compiles clean (`clang -fsyntax-only`, 0 errors/warnings) and stays
**macro-for-macro identical to the source** (header-parity: 0 `#define`/`#undef` differences).
`ScilabToolchain.cmake`'s guard, which required the source `machine.h`, now checks a *tracked*
template (`version.h.in`) instead, so a fresh clone passes.

**Verified:** with both source headers renamed away (fresh-clone simulation) `cmake` configures
(exit 0, no stale FATAL) and `scicore-obj` + `scijvm` compile against the generated copies; a full
`drop-in-all` build with the source headers absent is the acceptance test. The gitignored source
copies are now legacy — present but unused (the prepended generated dir wins), and safe to delete.

---

## 5b. `make install` had no replacement — RESOLVED 2026-07-23

Found 2026-07-21 while auditing the scripts; fixed 2026-07-23. Retiring autotools removed
`make install` and the CMake build defined **zero `install()` rules**, so the build was in-tree only
and could not be prefix-installed (what a distro package needs).

**Fix:** `cmake/ScilabInstall.cmake` (wired into `CMakeLists.txt`) adds the rules, so
`cmake --install build-cmake --prefix DIR` now installs a runnable Scilab at DIR and rewrites the
source-tree paths baked into the built config (`etc/classpath.xml`'s 59 absolute paths,
`jvm_options.xml`, launcher wrappers, `*.properties`) to the install location — the copy+relocate
`package-macos.sh` already does for the `.app`, exposed as the standard CMake install so it works on
any platform and any prefix. **Verified:** install to a temp prefix succeeds (exit 0), relocation
leaves 0 source-path refs, and Scilab runs from the installed tree (`SCI` resolves to the prefix,
`det`/macros work, exit 0). Parity 210 passed — the rules are configure-time-only, build-neutral.
Scope: a runnable SCI-rooted tree; the leaner autotools-style FHS split (bin/ + lib/scilab/ +
share/scilab/ + include/scilab/) that `bin/scilab` still supports is future work.

Original finding, for context:

This was invisible during the migration because the parity harness compares *build outputs* in the
tree, and macOS packaging (`package-macos.sh`) rsyncs a relocated copy of that tree — neither ever
calls install. It is a genuine capability regression, not a cosmetic one:

| Consumer | Effect |
|---|---|
| `.gitlab-ci/build.sh` | **Structurally broken past the install step**, not merely stale. Everything from the `patch binary` section on reads `/tmp/${SCI_VERSION_STRING}`, which only `make install DESTDIR=` populated. Now fails loudly with an explanatory message instead of silently packaging an empty tree. |
| `bin/scilab` | Its runtime search logic still supports the split-prefix layout (`/usr/local/{bin,share/scilab,lib/scilab,include/scilab}`) and the DESTDIR variant Linux packagers use. That code is live and must not be deleted — but **no build path in this tree can emit those layouts any more.** Header comment corrected to say so. |
| Linux/distro packaging | Not possible from this tree until install rules exist. macOS is unaffected (`package-macos.sh`). |

**Deferred: add `install()` rules to the CMake build.** Not urgent for this fork (macOS packaging
works without them), but it is the honest scope of what "autotools retired" cost, and it must be
closed before anyone packages this tree for a distro.

---

## 5c. CI pipeline — validated statically, and it CANNOT build this tree

Audited 2026-07-21 after an initial dismissal of `.gitlab-ci/prebuild.sh` as "third-party, not
ours" was correctly challenged. It is in this repository, so it is ours. Validating it found two
hard blockers that the dismissal would have missed.

**What static validation proves.** All four `.gitlab-ci/*.sh` parse (`bash -n`) and are
**shellcheck-clean at warning severity and above** — 0 findings; `prebuild.sh` has only 11
style-level notes and carries 14 explicit `shellcheck disable` directives, i.e. upstream lints it
deliberately. The shellcheck run was itself verified against a known-bad script (it reported
SC2154) rather than trusted for reporting zero. The scripts are well-maintained.

**What static validation cannot prove — and here does not hold: that they still WORK.** The image
they build predates the migration and is now incompatible with it:

| # | Blocker | Evidence | Effect |
|---|---|---|---|
| CI-1 | **No Maven anywhere in CI provisioning.** | Zero case-insensitive matches for `maven`/`mvn` in `prebuild.sh` and `docker_setup.sh`; `Dockerfile.linux`'s apt list has none. | `cmake/ScilabJava.cmake` resolves `find_program(SCILAB_MVN mvn)` off PATH and FATALs at point of use. The 24 module jars cannot be built. Repointing `build.sh` to CMake is necessary but **not sufficient** — the image lacks the tool. |
| CI-2 | **JDK 17, but the reactor compiles at source 25.** | `prebuild.sh:84` `JDK_VERSION=17.0.7+7`; `Dockerfile.linux:136` `openjdk-17-jdk`. Root `pom.xml:132` `<source>25</source>`. | javac 17 rejects source 25. Not a one-line bump: Debian bookworm has no openjdk-25, so it needs a JDK 25 tarball on the Scilab artifact mirror — **infrastructure this fork does not control**. |
| CI-3 | **Apache Ant still downloaded and installed** (`prebuild.sh:182,629-640`), and `ANT_HOME` exported. | Nothing consumes it: all 26 `build.xml` were deleted. | Dead weight in the image. Harmless, but it misleads a reader into thinking Ant is still part of the build. |

CMake itself **is** provisioned (`Dockerfile.linux.prebuild` builds it from source), so that half of
the migration is already satisfied. The autotools packages (`autotools-dev automake libtool`) stay:
third-party dependencies built from source in the image still need them, even though Scilab does not.

**Not fixed here, deliberately.** CI-1 and CI-2 are Linux-image changes that cannot be verified on
this machine — this fork's pipeline is disabled and upstream CI needs Dassault runners — and CI-2 is
additionally blocked on an artifact mirror we do not control. Writing plausible-but-unverified
Dockerfile edits would replace a *documented* blocker with a *hidden* one. Documented instead, so
re-enabling CI starts from a known list rather than a mystery.

**Fixed here:** the reachable-but-dead `-DSCILAB_JAVA_BUILD=ant` path in `cmake/ScilabJava.cmake`.
It would have located ant, launched it, and died inside ant with "Buildfile: build.xml does not
exist!" — blaming ant instead of the retirement. It now fails at configure time naming the real
cause. Verified in both directions: ant mode exits 1 with the new message, default maven mode still
configures clean (exit 0).

---

## 5d. JOGL is still the production graphics path — do not remove it

Raised 2026-07-21: since the Vulkan renderer landed, is JOGL still needed? **Yes.** Measured:

- 4 JOGL/GlueGen jars in `thirdparty/`, referenced by `etc/classpath.xml`
- **6** module POMs depend on JOGL
- **30** Java sources import `com.jogamp`
- `scirenderer` carries **both** implementations side by side — `implementation.jogl.*` and
  `implementation.vulkan.*` (see `modules/gui/pom.xml:36`)

The Vulkan renderer ([[realtime-3d-renderer]], merged `d30f75059e5`) was added **alongside** JOGL,
not in place of it. Removing JOGL is not a build-script cleanup; it requires porting 30 source files
across 6 modules and proving feature parity on every graphics surface. So `prebuild.sh` building
JOGL is **correct and still required** — the right call for the right reason, which is that it is
load-bearing for us, not that it is somebody else's code.

---

## 6. Downstream scope — the toolbox ecosystem (audited 2026-07-21)

Asked after the endgame: *"are the toolboxes built with Ant? If so they should be built with Maven
too."* Measured across the ~50 toolboxes in `SciLabProjects/`. **The intuitive half of the answer is
the empty one, and the real exposure is somewhere else.**

| Finding | Measurement | Consequence |
|---|---|---|
| **Toolboxes never used Ant.** | **0** Ant build files. The only 3 `build.xml` on disk are vendored **SWIG Android examples** under `sciQuantLib/swig/Examples/` — third-party sample code, never executed by any Scilab build. | Retiring Ant (`42e58dd3707`) cost the toolbox ecosystem **nothing**. No follow-up needed. |
| **Toolbox Java barely exists.** | **2** of ~50 toolboxes contain any `.java`: `swing-gpu-surface` (first-party, **already Maven**) and that same vendored SWIG tree. In-tree, `contrib/toolbox_skeleton` has exactly **one** `.java`. | **Do not Mavenize toolbox Java.** Forcing toolbox authors to install Maven to ship a jar is a usability regression against a population of one file. `ilib_build_jar` (`modules/dynamic_link/macros/`, a Scilab macro — no Ant, and no `javac` shell-out; a grep for "javac" false-positives on `javaclasspath`) is the correct Scilab-native API. |
| **The real legacy dependency is autotools — and it is LIVE, at runtime.** | `modules/dynamic_link/src/scripts/` ships a complete skeleton: `configure`, `configure.ac`, `Makefile.am/.in`, `aclocal.m4`, `ltmain.sh`, `config.guess`, `config.sub`, `depcomp`, `compile`, `missing`. Toolboxes compiling native code via `ilib_build`/`tbx_build_src` run **`./configure && make` on the end user's machine** (`ilib_compile.sci:157,165`). Preserved deliberately in the autotools purge — `1ed4171fad1` deleted **0** files under that path. | **This is the last live autotools in the tree** and the true parallel to the finished core migration. Deleting it breaks every toolbox's native build, so it was correctly kept. |

**Deferred: `ilib_build` → CMake.** Needs its own spec, not a task on this register. It is
higher-stakes than anything in the core migration for two reasons the core work never faced:
it **executes on end-user machines** (so a defect ships as a broken toolbox build, not a broken
local tree), and **toolboxes ship prebuilt binaries in the wild**, making backward compatibility a
hard constraint rather than a nicety. The parity question also inverts: the artifact to hold
constant is a *third-party toolbox's* built dylib, which this repository does not contain — so the
gate must be built from the toolbox catalog before any code moves.

---

## Known-unknown, worth stating

**Maven resolution here depends on machine-local configuration the repository does not carry.**
`~/.m2/settings.xml` mirrors everything to a Nexus that answers 401; resolution succeeds only via
an Azure DevOps feed excluded from that mirror. A fresh clone on another machine has none of this.
That is not a Scilab defect and not a harness gap — it is a portability gap that must close before
the endgame, since "delete autotools" implies the Maven build works for someone who is not us.
